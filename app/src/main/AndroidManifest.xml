<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sirionrazzer.diary">

    <application
        android:name=".Diary"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".history.HistoryActivity" />

        <activity android:name=".main.MainActivity" />
        <activity android:name=".creator.TemplateItemCreatorActivity" />
        <activity android:name=".viewer.TemplateItemViewerActivity" />
        <activity android:name=".stats.TrackItemStatsActivity" />
        <activity android:name=".boarding.BoardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
    </application>

</manifest>